Enhancements / Suggestions

- Expand the skeleton to include structured JSON outputs and an example test harness that exercises the `example_tool` via the MCP protocol.
- Provide a small CLI wrapper that runs the server and performs a sample `tools/list` and `tools/call` for developer convenience.
